# Copy to .env and fill in. Anything set here overrides appsettings.json.
# Nested keys use a double underscore (Section__Key).

WEB_PORT=5080
ASPNETCORE_ENVIRONMENT=Production

# ConnectionStrings__DefaultConnection=Server=db;Database=Daleel;User Id=sa;Password=Daleel_Local#2026;Encrypt=True;TrustServerCertificate=True;MultipleActiveResultSets=True;
# Gemini__ApiKey=
# RagApi__BaseUrl=http://host.docker.internal:8080/
# CrmSeed__AdminEmail=admin@daleel.local
# CrmSeed__AdminPassword=

# Only used by the optional local SQL Server (docker compose --profile local-db up -d)
# MSSQL_SA_PASSWORD=Daleel_Local#2026
