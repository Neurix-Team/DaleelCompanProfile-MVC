@model Daleel.Models.BrandProfileFormViewModel
@{
    Layout = "_CmsLayout";
    ViewData["Title"] = $"Edit Brand: {Model.NameEn}";
}

<div class="max-w-4xl mx-auto space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <div>
            <div class="flex items-center gap-2 text-xs font-semibold uppercase tracking-wider text-slate-400 mb-1">
                <a asp-controller="CmsBrand" asp-action="Index" class="hover:text-[#00B2EC]">@SharedLocalizer["CmsBrands"]</a>
                <span>/</span>
                <span>@SharedLocalizer["CmsEdit"]</span>
            </div>
            <h2 class="font-poppins text-2xl font-bold text-brand-navy dark:text-white">
                @SharedLocalizer["CmsEditBrandPrefix"] @Model.NameEn
            </h2>
        </div>
        <a asp-controller="CmsBrand" asp-action="Index"
           class="inline-flex items-center gap-2 px-3.5 py-2 rounded-xl border border-slate-200 dark:border-white/10 text-slate-600 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-white/5 text-sm font-medium transition-colors">
            <span class="material-symbols-outlined text-[18px]">arrow_back</span>
            <span>@SharedLocalizer["CmsBackToBrands"]</span>
        </a>
    </div>

    <form asp-controller="CmsBrand" asp-action="Edit" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <partial name="_BrandForm" model="Model" />
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
